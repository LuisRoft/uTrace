import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    flagsContainer:{
        backgroundColor: '#fff',
        padding: 8,
        borderRadius: 25,
        width: '100%',
        alignItems: 'center',
        
    },
    flags:{
        fontWeight: 'bold',
        fontSize: 10
    }
});