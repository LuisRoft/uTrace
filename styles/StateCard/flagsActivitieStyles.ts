import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    flagsContainer: {
        width: '32%',
        padding: 5,
        borderRadius: 10,
        alignItems: 'center',
        justifyContent: 'center',
        marginHorizontal: 2,
    },
    flags: {
        fontWeight: 'bold',
        fontSize: 8,
    }
});
