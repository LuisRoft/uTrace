import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    fontSize: 16,
    color: '#333333',  
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 10,
    padding: 8, 
  },
});