import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    form: {
      width: '100%', 
      marginTop: 40, 
      alignItems: "center"
    },
    subtitle: {
      marginBottom: 20,
      alignSelf: 'baseline',
    },

    formButton: {
      marginTop: 30,
    }
  });