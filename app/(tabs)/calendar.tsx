import {View, Text} from 'react-native';
import { ThemedText } from '@/components/ThemedText';

export default function Calendar() {
    return (
        <View>
            <ThemedText type='title'>Calendar View</ThemedText>
        </View>
    )
}